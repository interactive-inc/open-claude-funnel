export const help = `funnel profiles <name> as-default — move profile to the front of the list

usage: funnel profiles <name> as-default

the first profile in the list is treated as the default for fnl claude.`
