// Docs sub-entry: embedded funnel documentation as a programmable API.
// FunnelDocs is dependency-free and safe to use anywhere — including inside
// a custom CLI that wants to surface the same docs as `fnl docs <topic>`.
export * from "@/engine/docs/funnel-docs"
